# ═══════════════════════════════════════════════════════════════
# COMPANION — Git Ignore
# Protects API keys, secrets, and deployment-specific configs
# ═══════════════════════════════════════════════════════════════

# ── Deployment config files ──
# config.js files use proxy mode (no secrets — just the worker URL).
# They are committed so they deploy with the site.
# The API key lives server-side in the Cloudflare Worker.
# If you switch to direct mode with an apiKey, do NOT commit that file.
Take_Action/config.js

# ── Environment files ──
.env
.env.*
*.env

# ── OS artifacts ──
.DS_Store
Thumbs.db

# ── Editor artifacts ──
*.swp
*.swo
*~
.vscode/
.idea/

# ── Python artifacts ──
__pycache__/
*.pyc
